<view class="container">
  <view class="content">
    <view class="logo-section">
      <image class="logo" src="/assets/images/banner/sheep.jpg" mode="aspectFit"></image>
      <text class="logo-text">Sleep</text>
    </view>

    <view class="login-section">
      <button class="login-btn" bindtap="handleLogin">一键登录</button>
      <view class="register-link" bindtap="goToRegister">暂不登录</view>
      
      <view class="privacy-section">
        <checkbox-group bindchange="handlePrivacyChange">
          <label class="privacy-label">
            <checkbox value="agreed" checked="{{isPrivacyChecked}}" />
            <view class="privacy-text-container">
              <text class="privacy-text">我已阅读并同意</text>
              <text class="privacy-link" bindtap="showPrivacyPolicy">《小程序隐私保护指引》</text>
              <text class="privacy-text">、</text>
              <text class="privacy-link" bindtap="showUserAgreement">《注册协议》</text>
              <text class="privacy-text">和</text>
              <text class="privacy-link" bindtap="showPrivacyAgreement">《隐私政策》</text>
            </view>
          </label>
        </checkbox-group>
      </view>
    </view>
  </view>

  <!-- 隐私协议弹窗 -->
  <view class="privacy-popup" wx:if="{{showPrivacyPopup}}">
    <view class="privacy-popup-mask"></view>
    <view class="privacy-popup-content">
      <view class="popup-title">请阅读并同意以下条款</view>
      <view class="popup-text">
        我已阅读并同意
        <text class="popup-link" bindtap="showPrivacyPolicy">《小程序隐私保护指引》</text>、
        <text class="popup-link" bindtap="showUserAgreement">《注册协议》</text>和
        <text class="popup-link" bindtap="showPrivacyAgreement">《隐私政策》</text>
      </view>
      <view class="popup-buttons">
        <button class="popup-btn disagree" bindtap="handleDisagreePrivacy"><text>不同意</text></button>
        <button class="popup-btn agree" 
          open-type="getPhoneNumber" 
          bindgetphonenumber="handleAgreePrivacyAndGetPhone"><text>同意</text></button>
      </view>
    </view>
  </view>

  <!-- 手机号获取弹窗 -->
  <view class="phone-popup" wx:if="{{showPhonePopup}}">
    <view class="phone-popup-mask"></view>
    <view class="phone-popup-content">
      <view class="phone-popup-content-aligned">
        <view class="phone-popup-header-aligned">
        <image class="phone-popup-logo" src="/assets/images/banner/sheep.jpg" />
          <text class="phone-popup-title">Sleep</text>
        </view>
        <view class="phone-popup-desc-container-aligned">
          <text class="phone-popup-desc-title">申请获取并验证你的手机号</text>
          <text class="phone-popup-subdesc">提供账号注册及登录功能</text>
        </view>
      </view>
      <view class="phone-popup-btns">
        <button class="phone-number-btn" bindtap="handlePhoneLogin" wx:if="{{hasWxPhone}}">
          <text class="phone-number">{{wxPhoneNumber}}</text>
          <text class="phone-label-green">微信绑定手机号</text>
        </button>
        <button class="phone-number-btn" bindtap="handleHistoricalPhoneLogin" wx:if="{{hasHistoricalPhone}}">
          <text class="phone-number">{{historicalPhone}}</text>
          <text class="phone-label-green">历史绑定手机号</text>
        </button>
        <button class="deny-btn" bindtap="handleUseWxPhone"><text>不允许</text></button>
        <view class="other-phone-container">
          <button class="other-phone-btn" bindtap="handleUseOtherPhone"><text>使用其他号码</text></button>
        </view>
      </view>
    </view>
  </view>
</view> 